# コードの生成

import { Callout, FileTree } from 'nextra-theme-docs'
import {Screenshot} from 'components/screenshot'
import CODE1 from '../../img/code-generation/chat-mode.png'

<Callout emoji="⚠️">
このセクションは開発中です。
</Callout>

ChatGPT のような LLM はコード生成に非常に効果的です。このセクションでは、コード生成に ChatGPT を使用する方法の多くの例を取り上げます。

OpenAI のプレイグラウンド (チャット モード) と`gpt-3.5-turbo`モデルは以下のすべての例で使用されます。

OpenAI のすべてのチャット モデルと同様に、`System Message`応答の動作と形式を定義します。プロンプトの例では、次のシステム メッセージを使用します。

```
You are a helpful code assistant that can teach a junior developer how to code. Your language of choice is Python. Don't explain the code, just generate the code block itself.
```

## 基本的な例

の`User Message`これは、モデルに生成させたい特定のコードの命令を含むプロンプトそのものになります。以下は、コード生成のための最初の基本プロンプトのスナップショットです。最終的なコードは、`Assistant`.

<Screenshot src={CODE1} alt="CODE1" />

自分で試してみたい場合のプロンプトは次のとおりです。

```
Write code that asks the user for their name and say "Hello"
```

## コメントをコードに変換する

これらの LLM のコード生成機能を使用して、コメントのみからコードを生成することもできます。命令をコメント ブロックとして渡す別の例を見てみましょう。

*プロンプト：*

```
"""
1. Create a list of movies
2. Create a list of ratings for these movies
3. Combine them to make a json object of 10 movies with their ratings.
"""
```

*出力：*

```python
movies = ["The Shawshank Redemption", "The Godfather", "The Dark Knight", "Schindler's List", "Pulp Fiction", "The Lord of the Rings: The Return of the King", "Forrest Gump", "Star Wars: Episode V - The Empire Strikes Back", "Inception", "The Silence of the Lambs"]

ratings = [9.3, 9.2, 9.0, 8.9, 8.9, 8.9, 8.8, 8.7, 8.7, 8.6]

movie_ratings = {}

for i in range(len(movies)):
    movie_ratings[movies[i]] = ratings[i]

json_object = json.dumps(movie_ratings, indent=4)

print(json_object)
```

生成されたコードは動作するコードのように見えます。ただし、出力には重要な情報が含まれていません。`import`ステートメント、つまり`import json`。これらのモデルは機能するコードを生成しますが、このような細かい点に細心の注意を払い、必ずコードをテストする必要があります。

## 完全な関数または次の行

これらの LLM は GitHub Copilot などのツールにも組み込まれており、開発者にとって便利です。便利な機能の 1 つは、モデルが関数を完了できることです。

*プロンプト：*

```python
# function to multiply two numbers and add 75 to it

def multiply(
```

*出力：*

```python
a, b):
    result = a * b
    result += 75
    return result
```

## MySQL クエリの生成

上記の基本的なコード生成例に加えて、このモデルを使用して、MySQL クエリの作成やテストなどのプログラミングの他の側面で使用できる便利なコードを生成できます。

プロンプトの一部として含めることができる情報を含むデータセットがあり、特定のクエリを生成するように指示するとします。例えば：

```
"""
Table departments, columns = [DepartmentId, DepartmentName]
Table students, columns = [DepartmentId, StudentId, StudentName]
Create a MySQL query for all students in the Computer Science department
"""
```

*出力：*

```mysql
SELECT students.StudentId, students.StudentName
FROM students
INNER JOIN departments
ON students.DepartmentId = departments.DepartmentId
WHERE departments.DepartmentName = 'Computer Science';
```

モデルがクエリを生成します。ただし、この例ではデータベースを作成したため、生成されたクエリをテストするためのデータベースがありません。この例をテストする場合は、このデータベースを作成しておくと便利です。幸いなことに、ChatGPT を使用してこれを生成することもできます。 1 つのアプローチは、データベース スキーマを生成するための手順がわずかに異なる、上記のプロンプトの修正バージョンを使用することです。

```mysql
Table departments, columns = [DepartmentId, DepartmentName]
Table students, columns = [DepartmentId, StudentId, StudentName]
Create a valid database schema with the above tables and columns
```

モデルは次のスキーマを生成しました。

```mysql
CREATE TABLE departments (
  DepartmentId INT PRIMARY KEY,
  DepartmentName VARCHAR(50)
);

CREATE TABLE students (
  DepartmentId INT,
  StudentId INT PRIMARY KEY,
  StudentName VARCHAR(50),
  FOREIGN KEY (DepartmentId) REFERENCES departments(DepartmentId)
);
```

次のようなものを使用して一時データベースを作成できます[sqliteonline.com](https://sqliteonline.com/).

元の生成されたクエリが有効かどうかをテストするためのダミー データも必要になります。これもChatGPTを使ってみましょう！プロンプトは次のようになります。

```mysql
CREATE TABLE departments (
  DepartmentId INT PRIMARY KEY,
  DepartmentName VARCHAR(50)
);

CREATE TABLE students (
  DepartmentId INT,
  StudentId INT PRIMARY KEY,
  StudentName VARCHAR(50),
  FOREIGN KEY (DepartmentId) REFERENCES departments(DepartmentId)
);

Given the database schema above, generate valid insert statements include 4 rows for each table.
```

insert ステートメントを使用した出力は次のとおりです。

```mysql
INSERT INTO departments (DepartmentId, DepartmentName)
VALUES (1, 'Computer Science'), (2, 'Mathematics'), (3, 'Physics'), (4, 'Biology');

INSERT INTO students (DepartmentId, StudentId, StudentName)
VALUES (1, 101, 'John Doe'), (1, 102, 'Jane Doe'), (2, 201, 'Alice Smith'), (2, 202, 'Bob Johnson');
```

データベースとダミー データが完成したので、元のクエリをテストできます。ダミー データに基づくと、結果は次の 2 行になるはずです。`Jane Doe`そして`John Doe`.

## コードの説明をする

特定の言語でのプログラミングを学習している場合は、モデルにコードの特定の部分を説明するよう促すと便利な場合があります。上記で生成されたクエリを再利用し、モデルに説明してもらいましょう。

以前と同じシステム メッセージを使用している場合は、モデルへの指示方法に注意してください。たとえば、次の例を使用すると、モデルは Python のコード アシスタントであると主張しますが、それでも説明は提供されます。モデルはシステム メッセージに従うように設計されているため、指示した内容の生成を拒否する場合があります。

*プロンプト：*

```mysql
SELECT students.StudentId, students.StudentName
FROM students
INNER JOIN departments
ON students.DepartmentId = departments.DepartmentId
WHERE departments.DepartmentName = 'Computer Science';

Explain the above SQL statement.
```

*出力：*

```
Sorry, as a code assistant in Python, I cannot generate SQL code. However, I can explain the purpose of the SQL statement provided. 

This SQL statement selects the `StudentId` and `StudentName` columns from the `students` table, where the `DepartmentName` column in the `departments` table is equal to 'Computer Science'. To achieve this, the statement performs an inner join on the `DepartmentId` column between the `students` and `departments` tables. 

In simpler terms, it is selecting the names and IDs of students who are enrolled in the Computer Science department.
```

## コードの編集

例は近日公開予定です。

## コードのデバッグ

例は近日公開予定です。

## ベストプラクティス

近日公開！
